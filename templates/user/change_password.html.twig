{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-3"></div>
        <div class="col-md-6">
            <div class="card">
                <div class="card-header text-center">
                    <h1>Change {{ user.username }} password</h1>
                </div>
                <div class="card-body">
                    {{ include('user/_form.html.twig', {'button_label': 'Change password'}) }}
                </div>
            </div>
        </div>
        <div class="col-md-3"></div>
    </div>
{% endblock %}